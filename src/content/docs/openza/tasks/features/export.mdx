---
title: Export to Markdown
description: Export your tasks to markdown format
---

import { Steps, Aside } from '@astrojs/starlight/components';

Export your current task data to a human-readable Markdown file for archiving, sharing, or migrating to other tools.

## Export Format

Tasks are exported in organized Markdown format:

- **Grouped by project** for easy navigation
- **Checkbox syntax** (`- [ ]` for open, `- [x]` for completed)
- **Task details** including priority, dates, deadlines, and labels
- **Separate labels section**

### Example Output

```markdown
# Openza Tasks Export

## Projects & Tasks

### Work

- [ ] Review quarterly report (Priority: High, Due: 2026-05-15) @work @urgent
- [x] Submit expense report (Priority: Medium)
```

## How to Export

<Steps>
1. Click **Export Markdown** in the command bar
2. Choose a save location and filename
3. Openza Tasks writes a `.md` file
</Steps>

## Export Preview

The exported file includes totals, grouped tasks, and labels.

## Use Cases

### Archiving
Create periodic exports to keep a readable history of your tasks outside the app.

### Sharing
Share task lists with team members who don't use Openza Tasks.

### Migration
Move your tasks to another tool that supports markdown import.

### Documentation
Include task lists in project documentation or reports.

## Notes

- Export includes tasks created in Openza and tasks connected to Todoist or Microsoft To Do
- Completed tasks are included with `[x]` checkboxes
- File is saved with `.md` extension
- Export uses the Windows file picker
- Account details, access tokens, backup passphrases, and backup files are never included
